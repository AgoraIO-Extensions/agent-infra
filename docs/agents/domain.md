# Domain Docs

当前采用 single-context 布局。

## Before exploring

按需读取根目录 `CONTEXT.md` 和 `docs/adr/`。文件不存在时继续工作，
不提前创建占位文件。

## Layout

- `CONTEXT.md`：全仓领域词汇。
- `docs/adr/`：全仓技术决策。

输出使用 `CONTEXT.md` 已定义的术语；与 ADR 冲突时显式指出。
